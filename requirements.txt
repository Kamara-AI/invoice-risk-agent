# ==============================================================================
# Invoice Risk Intelligence Agent — Python Dependencies
# Python 3.11+ required.
# Pin minor versions; patch versions are allowed to float for security fixes.
# ==============================================================================

# LangGraph / LangChain — agentic orchestration and LLM integrations
langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0
langsmith>=0.1.0

# FastAPI — REST API layer
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Pydantic — schema-first validation
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Supabase — persistent storage
supabase>=2.7.0

# Stripe — payment fingerprinting and Radar score
stripe>=10.0.0

# Google APIs — Gmail ingestion
google-auth>=2.35.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.150.0

# Slack — human review and alerts
slack-sdk>=3.33.0

# HTTP and form handling
python-multipart>=0.0.12
httpx>=0.27.0

# Environment variable loading
python-dotenv>=1.0.0

# Streamlit — demo UI
streamlit>=1.38.0
pandas>=2.2.0

# pypdf — PDF text extraction (parse_invoice node)
pypdf>=4.3.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
